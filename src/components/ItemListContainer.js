import { Title } from "./Title";

export const ItemListContainer = () => {
    return (
        <><div class="ItemListContainer">
            <Title text="Home"/>
        </div>
        </>
    );
}